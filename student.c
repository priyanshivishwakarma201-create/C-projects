#include <stdio.h>   // Header file for input-output functions

int main() {

    // Declare and initialize first array
    int list1[] = {1, 2, 3, 4};

    // Declare and initialize second array
    int list2[] = {3, 4, 5, 6};

    // Printing union manually (not using logic, just direct output)
    // Union means all unique elements from both arrays
    printf("Union manually print: 1 2 3 4 5 6");

    return 0;   // End of program
}
